const mdLinks = require('../index');


describe('mdLinks', () => {

  it('should...', () => {
    console.log('FIX ME!');
  });

  it('should return a promise', () => {
    expect(typeof mdLinks).toBe('function')
  });

});
